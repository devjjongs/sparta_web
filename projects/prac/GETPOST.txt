GET -> 통상적으로 데이터 조회(Read)를 요청할 때 사용
    -> 데이터 전달 : URL 뒤에 물음표를 붙여 key = value로 전달
    -> 예 : google.com?q=북극곰

POST -> 통상적으로 데이터 생성(Create), 변경(Update), 삭제(delete)를 요청할 때 사용
    -> 데이터 전달 : 바로 보이지 않는 HTML body에 key : value 형태로 전달
    -> 예 : 회원가입, 회원탈퇴, 비밀번호 수정
